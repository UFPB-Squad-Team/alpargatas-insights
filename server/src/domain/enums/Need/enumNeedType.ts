
/**
 * @enum NeedType
 *
 * @description Enum representing the type of need,
 * we can add more statuses in the future if needed.
 */

export enum NeedType{
    INFRASTRUCTURE = 'infrastructure',
    MATERIAL = 'material',
    HR = 'hr',
    SOCIAL = 'social',
    OTHER = 'other'
}