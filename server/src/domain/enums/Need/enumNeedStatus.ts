
/**
 * @enum NeedStatus
 *
 * @description Enum representing the status of need,
 * we can add more statuses in the future if needed.
 */
export enum NeedStatus{
    PENDING = 'pending',
    APPROVED = 'approved',
    REJECTED = 'rejected'
}